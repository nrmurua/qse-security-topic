Currently the action_build github workflow only transpiles the changelog re-usable action, however it should be updated to take a list of re-usable actions as there are now three and more are surely expected in the future.
