noisy is not respected. This should probably throw an error. Do either
or
h/t @joshcombes
see #783; this was actually a typo bug bug. The use case should (have) be(en) supported so it won't throw an error. Mix your flags to your hearts content!
