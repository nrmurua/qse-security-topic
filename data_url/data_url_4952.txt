new functions:
refactors:
unit tests for new functions:
add additional unit tests (for every func??) on a large state (e.g. 20 qubit), so ensure things like cross-block GPU communication works.
bug fix:
run ALL tests:
other:
