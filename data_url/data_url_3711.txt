I would expect the following to include 0.
I see now that the docstring says it only returns allocated qubits- is this the functionality that's most useful, rather than every qubit used in the circuit?
The plot thickens:
