When we .get u1, u2 or u3 from CmdDef, all FrameChange phases become positive value.
this returns a schedule consisting of FrameChange of +pi (because P0 is negated in cmd_def).
this returns a schedule consisting of FrameChange of +pi, but this should be -pi.
