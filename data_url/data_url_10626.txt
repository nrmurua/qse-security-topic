The fix in #478 ensures solver gets penalized for failing, but user's (or default) solver preference is ignored when selecting the next solver. As elaborated in #478, we want to respect user's order, but skip penalized solvers (if possible).
