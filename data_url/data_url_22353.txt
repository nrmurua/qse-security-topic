I was trying to run  artiq/artiq/examples/master/repository/histograms.py on 1.0 on Windows.
It seems like it needs a build function if not it doesn't load in the repository due to a NotImplementedError.
Also, I think the set datasets need to be after the for loop,
If not you get errors like this,
It seems like it needs a build function if not it doesn't load in the repository due to a NotImplementedError.
Please use the 1.0 version of the example with ARTIQ 1.0. Looking at the other problems now.
