Some of the code in sample.h and observe.h uses lambda expressions that are C++20 (so cannot be backported) but also cause failures in the CI. The failure are related to the async test with g++ 12 and debug builds.
