After #655, the local Python simulators accept a Qobj instance (with the new schema) during .run(), but internally they still use the old schema (for example, in .run_circuit()). Ideally they should also make use of the new schema and the instances directly instead of performing that conversion.
