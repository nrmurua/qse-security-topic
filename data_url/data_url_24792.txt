Currently, the method is_interruptable of SequencePulseTable does not work as intended. There should be a flag indicating whether or not the sequence of sub-PulseTemplates can be interrupted or not (instead of delegating the call to them).
