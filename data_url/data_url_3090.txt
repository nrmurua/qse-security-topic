Reported in Zendesk #1739
In the pyQuil documentation at:
https://pyquil-docs.rigetti.com/en/stable/compiler.html
the second example produces an error unless "np.metadata" is
replaced with "np".
Please see below.
Operating System:
Python Version (python -V):
Quilc Version (quilc --version):
QVM Version (qvm --version):
Python Environment Details (pip freeze or conda list):
