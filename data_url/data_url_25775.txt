Since in tests, the LocalExecutor is being used by default which is incompatible with the microservices code, thus in order to make the tests and default created electrons compatible with the new code we need to switch the LocalExecutor with the one in refactor folder currently.
