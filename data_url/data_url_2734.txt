This more strongly associates the information with the failing test, instead of globbing it all together at the end. We have several testing methods that essentially say if bad: print message, but they should be assert not bad, message
