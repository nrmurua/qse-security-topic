When drawing a circuit with >= 10 bits in a register the subscript is only applied to the first character in the index. For example:

The entire index should be included in the subscript, for example like:

Visualize a circuit with >=10 bits in a register using the latex output.
The subscript applies to all characters in the index
Fix the bug (it's a one line fix)
