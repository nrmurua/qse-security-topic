The reunification of the config files undid PR #1259, so that the default executor is always "local" regardless of whether Covalent was started with --no-cluster. That patch needs to be applied again with the appropriate modifications.
