Unit tests triggered by dependabot always throw API error e.g.
These updates pass unit tests when run locally, and even pass when same changes are made with separate PR. But for some reason they throw errors when run by dependabot actions.
Not exactly sure. Will have to look into it more.
