Random circuit was changed recently to make sure that gates that have too many qubits are not used in the random circuit.  This had the side effect that the sometimes removed all of the gates from the domain, which causes test_random_circuit to fail.
