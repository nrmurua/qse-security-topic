Currently response validation silently passes if there's no Content-Type header present. This makes (mock) testing easier, but requiring (correct) response type (for non-error responses at least) might make more sense in a prod setting.
