This is very useful when testing optimizations. The notion of equivalence often needs to include concepts like "don't care about extra Z operations on a qubit if that qubit is measured", although that really only makes sense in the context of otherwise-unitary circuits with terminal measurements.
