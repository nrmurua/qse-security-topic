With a Qiskit executor, instead of sending a function to be executed, a Qiskit circuit (along with other required parameters) is sent to either the local Qiskit simulator or actual quantum hardware.
Inputs to the initialization should be:
Inputs to the execute method should be:
