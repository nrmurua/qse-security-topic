When running simple circuits like an ALL X the exponential map introduces a significant overhead over simple Quil code that could just leverage native RX gates that are available in the pyQuil gate set.
This would reduce program overhead significantly.
