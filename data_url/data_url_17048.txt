The qiskit package should work with marshmallow >= 0.3. Currently it only works with marshmallow < 0.3, which generates problems with other packages requiring a recent version of marshmallow.
Yes, it seems marshmallow is finally out of RC - that was the main reason we were sticking to the 2.x line.
