If we can take a py::object, we can check if it has a .qasm()/.out() attribute, which means its a qiskit QuantumCircuit/ pyquil Program, and use that to enable a python api that can execute qiskit and pyquil circuit representations, without the boilerplate needed to write and read qasm strings.
