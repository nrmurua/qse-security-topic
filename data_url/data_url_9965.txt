Either by removing the dependency altogether, including the headers or by finding a way to pip install it.
Having a dependency on boost complicates source installs. For instance, right now the documentation build it failing because boost cannot be installed by RTD.
See also #519
