dwave setup will wrap dwave install and dwave config create.
Both setup and install command can temporarily live in the cloud client, until we modularize the CLI in Ocean, so each package can register its subcommands of dwave command.
