It would be nice-to-have feature to have editable (default) values when asking for user input in dwave configure.
For now using readline on Linux, but when making the client fully Windows-compatible, we'll need to figure out some solution there (or fallback to non-editable inputs).
