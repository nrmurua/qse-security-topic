When placed on the circuit you can move its left and right sides so that it covers gates. It always covers the height of the whole circuit. It disables any embedded displays (though it'd be neat if they showed up inside the constructed gate.... nah).
Expose options for combining gates into a single gate:
Going with a button->dialog approach instead.
