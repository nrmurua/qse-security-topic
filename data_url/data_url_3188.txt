This was only partially completed: #445
Need to do the rest of the gates/measures and then figure out how the imports would work (so that people could choose this syntax or the regular syntax)
neat-o
This should also include a magic directive for memory declarations. I would suggest DECLARE, except that we do not have a corresponding pyquil.gates.DECLARE. Likewise, the tests should be updated to include memory declarations.
