Currently, to build a pass plugin for cudaq-opt, we need to the full CUDA Quantum source.
We need to figure out how an external user can build custom pass plugins apart from CUDA Quantum (without requiring a fork of the main repo).
Ref: #1034 (comment)
