A setting can currently be set either by setting the class variable as for example:
or by the method set_setting as for example:
which updates the file config/settings.ini.
However it would be good if there is a single way that does both.
