For the following gate params, the QC opt model reports infeasibility with default settings. However, setting commute_gate_constraints = false seems to keep the model feasible. Possibly, some constraint is cutting off the U3_2(0,0,pi/4) solution, which is optimal.
