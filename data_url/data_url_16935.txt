Pulse schedules should support parameters that allow parameterization of the schedule outside of timing. This would allow schedule to consume the Parameters of a QuantumCircuit and produce a Schedule containing these Parameters which could later be fixed.
