Due to the deprecation of Python 3.6 in the 0.16.0 PennyLane release, Lightning requires updating all Python versions to match. This currently causes tests to fail with the Ubuntu latest wheel build (see PR #104 (comment) )
Tasks:
Let me make an update to adjust this. 🙂
