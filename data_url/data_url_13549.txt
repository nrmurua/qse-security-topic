As noted in setup.py, argument install_requires should be a subset of requirements.txt, but is defined independently. This should be pulled from requirements.txt to be more robust. (So that, e.g., if we need to bump the Cirq dependency, we only need to do it in one place.)
