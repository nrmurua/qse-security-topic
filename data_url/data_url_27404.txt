Problem: Currently the users of the tool build and install scripts have no option to decide where the tools are installed. The default paths are used in any case.
Fix: Allow the -i flag to carry a parameter the sets the install path for the tool. Mirror this changes in the documentation.
