The first line of apply_clifford_to_pauli contains the expression list(zip(*list(pauli_in.operations_as_set()))). When pauli_in is set to sI(0), this throws an error, presumably because .operations_as_set() is empty and hence too small.
