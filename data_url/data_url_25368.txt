Acceptance Criteria
persist will also need to be moved to the result updater, as well as methods that call insert etc.
Instead of a ResultUpdater class, we will simply add a module with these functions in it
