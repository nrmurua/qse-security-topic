It's needed by the protobuf messages. Also, it would be nice if there's a way to change the travis configs to test that things work with only the runtime requirements installed (plus pytest, I guess).
Fixed by #466
