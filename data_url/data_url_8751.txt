There is currently a single file which stores the settings. It might be better to have two, one which is read by the modules and another for setting settings. This is to not overwrite someones settings in for example the unittests.
