When adding blockgates, ancilla qubits are sometimes required. We should check if such a gate is added to a step to a program, that the program contains enough qubits to deal with this.
Fixed by #108
