It is natural for executor instances to modify their own attributes at runtime. For instance, they may wish to persist some state throughout their lifecycle. Unfortunately, these modifications also change the transport graph nodes' metadata specifying the executor attributes. This linkage is unacceptable because
The problem lies in how the dispatcher reconstructs executor instances from the node metadata. The Executor.from_dict() method needs to make a deep copy of the node metadata.
