Currently density matrices are only supported by the slow gates that use tensorflow primitives. If we want a faster density matrix simulator we have to extend our custom operators to density matrices. This would require to write new specialized kernels both for CPU and CUDA.
