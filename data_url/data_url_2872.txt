Currently, you can't do this by providing a matrix or by providing a decomposition that uses arithmetic, so it seems to be unsupported (but a crucial feature). I propose some solutions at #393 and #418 .
