This can now be done in the measurement transformers, which also handle classically controlled operations. The simulator option does not (and can not) handle classical controls. This no longer needs to be a simulator option.
From Cirq sync:
We've accepted this, but if anyone who uses the density matrix simulator a lot has a differing view, please state it here.
