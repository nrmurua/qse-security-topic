this is evident e.g. when the NA gui is shown during nosetests.
Why do we actually want to support the curve() function instead of single(), the latter being shown in the GUI as one would expect when the instrument is working on an acquisition?
