Abstract the logging layer, as currently implemented in dwave-cloud-client (with support for env modifications). Extract perhaps to ~dwave-cloud-common package, together with some other generic utilities, so it can be consistently used across Ocean stack.
