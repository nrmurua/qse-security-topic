There is no test file for toqito/helper/kp_norm.py in tests/test_helper. But even then, only L20 is flagged as uncovered by tests instead of both L20 and L23.

After f51a6d4, L20 is still the only line flagged as uncovered.

