Create representation methods in jupyter notebook for several common objects such as the qiskit.result.result.Result,  Backend, BackendStatus (there are many more).
An example for the BackendStatus is (see #6341):

For the Result structure one could highlight the keys to make the structure more readable:

