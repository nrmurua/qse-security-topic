There is currently no way to dictate how an Observable is partitioned into qubit-wise commuting sets. Right now, they are partitioned via a greedy algorithm.

One may wish to specify exactly how PauliStrings are grouped and measured for more control when using Mitiq. We should support this.
