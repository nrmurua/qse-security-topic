This is the ability to rotate multiple qubits, by an arbitrary angle, around an arbitrary axis, with any number of control qubits.
The analogous QuESTlink syntax is
where _ indicates subscript.
This can be done by adaptation of multiRotatePauli and simply controlling every constituent unitary there-in invoked, exploiting that
added in PR #287
