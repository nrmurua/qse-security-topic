example from a config file:
yaml is able to properly format the data in a hierarchical way. Why do data appear like this?
problem seems to be only related to ruamel, not yaml
solved by deactivating ruamel support for now (makes package more lightweight)
