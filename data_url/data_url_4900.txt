Make a PR with some demo function which demonstrates the architecture (e.g. hardware code, front-end, validation, common-code, etc).
A simple example is in PR #207 though it is not yet referenced from any doc/README
