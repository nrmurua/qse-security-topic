for backlog, currently we bump version for each single packages, which triggers a lot of unnecessary CI builds, we should just bump all the package every time and release to the registry. I need to update the CLI tool for this.
