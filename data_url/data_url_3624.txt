As is the pyquil.noise.apply_noise_model prepends a full set of headers to a program regardless of which operations/qubits actually appear in the program. It would be more economical to only include the relevant definitions.
