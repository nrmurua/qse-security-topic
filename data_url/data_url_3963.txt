It is always called as program.set_sweep_points(sweep_points, len(sweep_points)). Having the second argument should be avoided, since it brings zero information to the function call.
Simplified in #197.
