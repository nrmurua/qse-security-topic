Inspired by #430. For debugging/testing, we should be able to sample the same circuit(s) every time. The root change should be in sample_circuit and the seed arg should percolate up to sample_circuit and execute_with_pec.
