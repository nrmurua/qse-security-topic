The following code:
produces a frame that looks like this:

What is that line along the bottom? Presumably it's one of the stabilizers, but it's been drawn as a line. It should be drawn with curvature so it has interior. Likely this requires a new colinearity check somewhere.
