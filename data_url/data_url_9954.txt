Add a new Vartype.Integer. This would allow us to handle integer quadratic models and discrete quadratic models.
Obviously this would be changing a core assumption in dimod so must be done very carefully.
