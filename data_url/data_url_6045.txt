Allow configuring simulator backends with persistent backend options and noise models.
Allow building mock qasm and pulse backends from real backend objects that automatically use the backend NoiseModel or PulseSystemModel.
