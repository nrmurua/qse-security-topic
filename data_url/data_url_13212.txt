Seeing the following warnings on CI:
First and last issues want us to directly use the qiskit-aer package, and the second should be fixed by removing the warnings key word argument.
I can fix this issue when I fix #1835
@andre-a-alves it looks like this was resolved by #1847, right?
@andre-a-alves it looks like this was resolved by #1847, right?
Yes, it was
