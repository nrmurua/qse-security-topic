For the following code:
I get error:
In wavefunction classical_addresses is an optional argument with None as default.
It seems that the following code inside from_bit_packed_string fixes the issue, however, probably it's a hack more than a solution.
