The QtPlot object is created using pyqtgraph multiprocessing. For some reason this gives lots of errors when using the object. (Both in Jupyter notebook and Spyder). For example:
Solved if the plotting window is created locally (#113).
