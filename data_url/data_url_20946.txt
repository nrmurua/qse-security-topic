The upcoming hard fork version will have an optional address to field in message transaction. Currently, the incoming messages are not tracked separately for explorer purpose. Thus, the node will be tracking the incoming messages for a qrl address sent via a message transaction which will be exposed via API.
Implemented in PR #1656
