In terra there are multiple seeds so the seed for the simulator was renamed seed_simulator. We need to make sure that if aer is passed this it uses it.
We also need to make sure the online simulator is the same.
I would make it accept both seed_simulator and see
