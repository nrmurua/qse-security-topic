Someone mentioned that Quirk looks like a static image, instead of something you interact with. I can see where they're coming from.
Look into how "interactivity" is usually hinted (e.g. shadows on the gates in the toolbox?).
ae08feb
