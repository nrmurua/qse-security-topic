Currently, the JobV1 abstract interface does not define a method called error_message, however some Qiskit functionality (e.g. here in qiskit_experiments) assumes that job objects have such a method. I think, it would be nice if the Job interface explicitly defined this as abstract method.
