As of v0.200.0, each transport graph edge now appears twice in the DB. This occurs because some logic to check for existing edge entries in the DB was accidentally removed during the recent reorg of DB logic.
