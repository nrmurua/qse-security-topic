We currently support SWAP operations at the builder level, down through to QIR. We should support controlled SWAP operations as well (see here in code). These are supported at the QIR level (see NVQIRTester.cpp), so it should primarily require changes to /lib/Optimizer/CodeGen/LowerToQIR.cpp.
