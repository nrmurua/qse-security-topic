Currently the version comes from setup.py as version="0.1.3" rather than version=version so, for example, ":Release: |version|" displays as 0: http://minorminer.readthedocs.io/en/latest/
Version comes from conf.py version = u'0.0'
Needs to be stored in one place, as in other projects
