One of the main uses cases of QCS, rapidly iterating over some set of parameters (a.k.a. hybrid quantum-classical programming), is only realizable through peeking into the QuantumComputer.qam attribute and directly writing memory. The run method should be extended to encapsulate this functionality.
