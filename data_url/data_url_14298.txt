"For example, in the preset pass managers, this stage is used to check compatibility of input instructions with the different passes in the transpiler, and convert custom instructions to a standard form." This reminds me we should probably carve out some of these passes into a canonicalization stage.
Originally posted by @kdk in #9087 (comment)
