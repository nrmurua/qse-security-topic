After the recent changes we have the following list of mentions of the QDK version in the Katas:
The current script Update-QDKVersion.ps1 handles the first three scenarios, plus the now-obsolete entries in the *.ipynb files that were removed in #468. We should add handling of the last two cases, similar to the other cases that handle a single file, so that future updates don't require manual updates of the comments.
