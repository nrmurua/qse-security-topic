PyQuil can do better than simply returning the JSON response to the user, most of the time people just want the result and don't care about things like jobId or time_in_queue
Also wavefunction should return an object instead of a tuple
