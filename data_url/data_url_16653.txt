circuit.qasm() outputs long float values for many parameters even though many are close to fractions of pi that can be checked with pi_check.  Also, the number of digits returned is excessive, and should be truncated to a reasonable precision.
