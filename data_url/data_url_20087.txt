README.md contains a list of dependencies required to build and run tests for liboqs. At least on Debian/Ubuntu, Python yaml module is required, albeit not listed in README.md. My suggestion would be to include python3-yaml on the apt command line excerpt needed to install the dependencies.
