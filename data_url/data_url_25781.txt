We need an action which which checks if all the pre-commit hooks have run successfully at every push. This is to ensure that even if someone does not have pre-commit hooks installed, that code should not become part of the codebase.
