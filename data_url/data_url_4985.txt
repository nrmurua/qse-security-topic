It's clear PR #117 was a hasty copy&paste job of the depolarising code - untested GPU code was buggy, as was the validation code, and so is distributed code (now properly unit tested). Even the CPU code used an arg differently than the doc-spec.
So
Patched in 2001043
