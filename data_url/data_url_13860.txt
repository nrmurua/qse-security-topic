I was just trying to run some examples of PyZX on the new Mac M1 and got no draw() output. I was wondering whether more people got the same issue or not. I'm currently using  Python 3.9.5 (latest pyenv + latest pipenv). Libraries installed so far in the env (everything's just fresh installed, so latest version):
The image was extracted from a local jupyter lab:

The following image was extracted from the pyzx/gettingstarted.ipynb at master example:

From Getting Started's page:
