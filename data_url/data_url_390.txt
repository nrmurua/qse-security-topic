For observe we can do this:
observe_n removes the for loop but then adds another one to retrieve expectation values:
Can we do something like:
I would say no since there may be more data associated with each invocation than just the expected value (e.g. the raw counts).
Closing as by design.
