Client.solvers() is documented, but it's not visible on RTC, because we're explicitly/manually creating one page per method.
Fixed. Also now moot since solvers method is deprecated in favor of get_solvers.
