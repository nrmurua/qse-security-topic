The reference ANTLR grammar does not have a parsing rule that matches cal statements.  This is a bug - it should know to tokenise the block into a single token and delegate the correct lexing and parsing to the defined calibration grammar parser.
