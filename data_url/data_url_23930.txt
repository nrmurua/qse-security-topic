Travis needs updating so that the tests stop failing on the load-from-yaml branch.
There's still some lingering weirdness in the builds where I need to restart them in Travis, but the tests themselves are passing now.
