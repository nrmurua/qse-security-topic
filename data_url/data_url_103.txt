The build script doesn't tell you if you hit errors. While building, all you see is the message that if there are errors, they'd logged into a file. If you succeed, you get a message. But if the build fails, it just returns.
Bumping this for visibility.
See below for examples of what happens in both cases.  You can see it is very unintuitive when the build script encounters an error.
when the build fails your terminal shows the following (just exits with no error mesage):
when the build succeeds the terminal shows the following (provides a confirmation message that the install succeeded):
