Running the unit tests distributed and GPU-accelerated will require our own hosting, and is a somewhat large task.
However, merely compiling for those backends to at least detect build errors can be done right away, in the same fashion as the MSVC build test.
