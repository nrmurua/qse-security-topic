Currently, running multiple instances of sinter unavoidably results in them pinning to the exact same CPUs. Add a command line argument to fix this; either an offset or a disable or an explicit list
Key locations are:
