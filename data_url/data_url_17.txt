Currently, the test/ directory contains both regression tests (quick verification of the IR) and compile-then-execute tests.
At some point we need to figure out how to separate nvq++ compile / execution tests (Remote-Sim and NVQPP folders) from the MLIR / LLVM FileCheck regression tests
#1012 (comment)
