This one seems tricky to deprecate, because its arguments are often ultimately from a user but are provided by intermediate methods such as cirq.sample. This makes it difficult to print a relevant error message in terms of what the user did, instead of the internal details of the library.
