The current multi-GPU supports only custom gates and therefore it does not have the following features:
All these features are available in the single device circuits only if the old (slow) tensorflow gates are used.
