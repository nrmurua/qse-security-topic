pytest is supposed to skip all tests sub-folders when it calculates coverage %.
But some of the tests do show up with missing tests. This issue is to make sure omit lines in coveragerc are entered correctly.


