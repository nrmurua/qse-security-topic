Implement a new AcceleratorDecorator in quantum/gate/accelerator that exposes an richardson_r option (number of CXs per a single CX), and updates all Functions to map CXs gates to richardson_r CX gates. It then delegates to its delegate Accelerator, and returns the resultant buffer(s).
