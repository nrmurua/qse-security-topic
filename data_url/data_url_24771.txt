Add type annotations to functions which are missing them (should be very few). Ideally, configure mypy and run a static type check with it to confirm consistency.
Check with mypy is now a separate issue.
