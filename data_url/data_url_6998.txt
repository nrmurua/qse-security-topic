Currently there's no way to tell.
Also it should show in the forge preview and in tooltip previews.
The default circuit drawer for gates should show the inner gates animating? (But don't animate the tooltips when not in use!)
2d2b685
