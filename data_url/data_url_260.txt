Currently the tools and the driver lack the ability to report the compiler version.
The driver nvq++ has no --version flag.
The tools will report LLVM's version:
We should change this and (also?) report CUDA Quantum's compiler version.
