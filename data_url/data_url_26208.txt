
For some reason, L147 shows up as uncovered even though there are tests that do test for int as input.

It might also be useful to change all the tests in the above linked file such that pytest.parametrize is used for all tests.
