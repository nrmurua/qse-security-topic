Several utilities got pulled into the google package along with the optimizers (because some were legitimately xmon specific). We should extract ones that make sense in other contexts back out if possible.
Is MergeRotations xmon specific?
MergeRotations doesn't exist anymore.
