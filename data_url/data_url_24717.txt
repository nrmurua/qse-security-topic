I.e. if a pulse uses the non-scalar parameter M[0] and M[1], it might be nice to map those two elements differently using a mapping pulse template (for example M[0] -> M[0]+a, M[1] -> M[1] + b).
-Pascal
This is already possible:
or
I will create an issue to document this.
