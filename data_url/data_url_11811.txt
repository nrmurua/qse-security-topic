Currently, Aqua's oracle creation requires the specification of the desired function in some particular formats (e.g. bitmap, cnf). A general way of creating oracle objects should also be added, namely direclty using quantum circuits supplied by users.
