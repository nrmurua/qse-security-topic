If you delete a dictionary arguments in the Param Entry Window, it will leave an empty dictionary as an arg
Create a Trasmon, add a nested dictionary to options then removed that added dictionary.
An empty dictionary arg will remain
Deleted dictionary should be completely removed
