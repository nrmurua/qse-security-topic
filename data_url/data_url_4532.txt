Given the number of backends we have, I think we should include a print/log statement when activating a backend, e.g. qibo {version} using {backend} {version} on {device}.
After discussion with @stavros11, we decide to keep the logging mechanism as it.
