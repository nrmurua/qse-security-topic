The current machine topology is currently specified via backend-specific topology files (i.e. --iqm-machine Adonis). The compiler and runtime then fetch the topology from $CUDAQ_INSTALL_PREFIX/targets/mapping/<backend>/<machine_name>.txt. It would be nice if the user could specify the full path to the file on the command line, perhaps like this:
