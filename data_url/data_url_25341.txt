Since this merge, benchmark workflows are failing. The failures appear to be 500 responses from the server, so this is a somewhat expected dispatcher breakage due to the DB work. Need to investigate where things are breaking exactly and confirm that the benchmarks can run.
