Could you please make them versioned, so that when a new file is available it would appear under a different, versioned name?
Currently the FreeBSD port lang/quilc breaks periodically because this file just changes.
