T2 should be calculated relative to T1 if it is not specified in add_decoherence_noise. Currently, if T1 is specified but T2 is not, an error is raised because T2 defaults to a constant scalar value. After doing this, we should update the documentation example in referenced by this comment
