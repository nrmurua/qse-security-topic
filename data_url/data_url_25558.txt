This is a followup issue to #748. We should add tests to check as long as the "local" executor is not used for either ordinary tasks or workflow_executor (postprocessing and dispatching sublattices), the dispatcher process never calls TransportableObject.get_deserialized().
