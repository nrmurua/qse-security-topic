The pydoc reads:
By default, only the notebooks under reference/ are automatically discovered (can be modified via the NOTEBOOK_PATH variable).
However, NOTEBOOK_PATH is set to qiskit/**/*.ipynb
Set NOTEBOOK_PATH to reference/**/*.ipynb. Alternatively, fix the pydoc.
