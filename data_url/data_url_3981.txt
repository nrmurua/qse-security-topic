If an instruction contains upper case characters in the config file, this is parsed to lowercase.
If then an instruction (gate) is specified in a program containing the upper case version (consistent with the config file), an exception is raised.

A workaround is to only use lower-case characters.
this can wait a bit. As a workaround only lower-case characters can be used.
closing because of lack of activity
