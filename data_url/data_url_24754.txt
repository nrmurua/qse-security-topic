Currently, RepetitionPulseTemplate simply repeats EXEC instructions when compiled into a sequence. However, some AWGs have a repetition functionality which should be used instead.
Consider how the current instructions and sequencing can be extended to support this.
