Should this be a global constant if its the safe for all kernels?
Should this be supplied at compilation to allow users to tune to their hardware? We can otherwise set it based on Compute Capability?
Makefile default can be auto:
https://devblogs.nvidia.com/cuda-pro-tip-occupancy-api-simplifies-launch-configuration/
