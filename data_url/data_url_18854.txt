When developing the language specification it is often necessary to break up work into digestible chunks, eg., first update the language specification and then update the grammar. This came up, in particular, to follow on work in #283. It would be desirable to have some sort of versioning strategy for the library so that we could decide when the repository is in a consistent state to release.
A potential way of doing this would be to a semantic versioning number for the language release of the form 3.x.x that is used to tag a Github release and build relevant packages through CI. Within the language itself this could then be specified as
