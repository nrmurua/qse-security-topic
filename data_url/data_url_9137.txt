Currently, there is no metric for the performance of QuNetSim, so it is hard to decide if a new changes significantly reduced the performance.
We need such a metric which runs on github at commits and reports the changes in performance.
