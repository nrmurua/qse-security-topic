The tagged version (v1.16.2) isn't available at atztogo/spglib, so the install process crashes when attempting to download the source.

The tagged version is still available on the main repository here.
