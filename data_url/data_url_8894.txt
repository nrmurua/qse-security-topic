This is just a list of all the gate names, so that e.g. downstream libraries can write tests that verify compatibility with all gates. There is currently a secret method with a prefix like __unstable_ that is exposing this information, but it would be useful to standardize into an API.
