The tests for merges simulaqron/tests/unittests/slow/merges used the old native mode to test register merges. We should update these to not use the old method of setting up a network since this is slow.
