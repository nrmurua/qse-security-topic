After "add_decoherence_noise" is added to a program, the new program ignores the original number of shots.
Expected behavior: The number of shots should be unaffected by the addition of  "add_decoherence_noise".
Run the code in the code snippet.
The program output should illustrate the issue as described above.
"Example Output" is provided below.
Operating System:
Python Version (python -V):
Quilc Version (quilc --version):
QVM Version (qvm --version):
Python Environment Details (pip freeze or conda list):
This will be fixed automatically for v4. Considering fixing it in V3.
