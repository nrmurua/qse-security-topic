The code below raises an error:
It compiles fine if kernel.h(qubits) is chosen rather than kernel.h(qubits[0]). However if I have more than 1 qubits, I need to address it as an element of a list rather than an int.
NA
The syntax should be the same for any number of qubits
Not a regression
NA
