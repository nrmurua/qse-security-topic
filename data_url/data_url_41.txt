There are still a few places, such as in the invariant loop builder, where the code uses mlir::IndexType that can be replaced with the i64 type. Removing the index type will help reduce a number of casts that clutter up the IR.
