In the example there is pm.BINARY but the pm module does not have BINARY or SPIN defined.
I have to import dimod and call BINARY and SPIN - not sure if this is a special case for me or just the example needs updating.
