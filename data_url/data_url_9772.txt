Inspired by zip's format, it might make sense for the offset, linear, quadratic, and variable labels (see #659) to have their own local headers. The offsets could also be stored in the header for easier reading, rather than being implicitly derived (example, example).
