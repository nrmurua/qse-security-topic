Something like
which would return a new sampleset containing the first n rows of the sampleset ordered by energy.
We could then do things like
Another similar feature would be to filter out some of the variables. Though a recalculation of the energy would be required.
