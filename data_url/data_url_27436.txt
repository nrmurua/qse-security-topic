Currently if E8267D_interface.FM_mode() == 'IQ, IQ modulation is always used, even if only a single frequency is needed.
It would be good to have an option to only use IQ modulation if needed
By adding DC pulses for IQ phase control we kind of fixed this issue, so would suggest to close it.
