From @smukesh25
qdesign.rebuild() function needs to be re-written to be faster
rewrite for speed
To implement this, some assumptions need to be made so some components are not rebuilt. For now, our users are used to rebuilding all of the design.
