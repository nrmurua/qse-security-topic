The pass manager drawer tests are currently specific to the flow in preset_pass_managers at level 0 and 1. These flows are unlikely to stay the same over time. The drawer tests should instead just build a small pass manager and draw that. Currently they fail every time we tweak the preset pass managers.
