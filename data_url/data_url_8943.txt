Fails if there are any non-clifford operations. Options to simply discard bad operations (basically same signature as stim.Tableau.from_circuit)
Reverses order of operations, reverses order of targets (pair chunking), switches gates to inverse
