Schmidt decomposition is already used "under-the-hood" in the shared library. However, there's some demand for an addition to the API that exposes Compose()/Decompose()/Dispose() directly to user code. This needs to respect the arbitrary bit labeling in the shared library, but this isn't particularly hard.
