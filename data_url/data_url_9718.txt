These functions should be better documented, and possibly promoted to the top-level namespace.
Further, it would be helpful for unpack_samples() to also accept an optional num_variables parameter. edit: that's the current n parameter. Hence the need for better docs 😆
