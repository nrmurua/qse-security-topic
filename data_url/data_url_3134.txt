The docs build is currently broken because nbformat broke their code. Pinning library dependencies is a no-no, but there should be no issue with pinning dev dependencies. In addition, the library dependencies should probably all have <= the next major version just in case people introduce breaking changes in their code.
