Our ARM64 implementations use CPU flags that don't appear available in the Qemu environment. Maybe QEMU does actually support these NEON instructions, or alternatively we might need to get a different testing setup.
