Description
When upper bound equals lower bound and are explicitly passed as arguments to the method, the lowest energy solutions violate the intended bounds.
Steps To Reproduce
Code to reproduce:
output:
However the same inequality where the bounds are subtracted out:
works fine:
Expected Behavior
The inequalities are equivalent so expected behavior is both have the same result as the second block of code.
Environment
Closed by #915
