There are various options for 'test_output' files that can be set to monitor progress of an optimisation or to debug. These are currently output to files during the run. By default none of these files are produced.
It would be better to return an object tree, which could then saved to file if necessary. It should remain possible to output to file during the run, as in some cases this can be useful.
Done
