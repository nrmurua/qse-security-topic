Using quantum volume circuits as mirror circuits ([QV][QV^dagger]) can be a simple way to provide benchmarks.
Add a module in mitiq.benchmarks with a function called
The content of the function could explicitly call the generate_quantum_volume_circuit() or copy some of that code.
