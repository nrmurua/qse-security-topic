In many cases i have created a property, and noticed that the setter is simply to be ignored.
At the moment this costs many lines of code, and is unnecessary. Creating a descriptor similar to @Property  that overrides the setter behaviour would solve this issue
