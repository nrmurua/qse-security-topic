Only allows output signals as inputs (as it it logical from the name). Accepting inputs brings no gain (inputs are already input_signals). Accepting any dsp module does not allow for a calibration, but is possible (calibration-less) with the class DirectInput (baseclass of InputFromOutput).
