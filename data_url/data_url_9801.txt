To find out how many unique variables a binary polynomial has, the user needs to
but the implementation of poly.variables creates a set 
It would be nice to track the number of variables as an attribute.
