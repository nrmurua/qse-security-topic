Each pulse template gets an optional keyword argument parameter_contraints=['a < 2', 'foo > sin(beta)'], a list of expressions (optional: each depending on at least one external parameter) of the pulse template, and two properties for access:
Pros:
Cons:
Solves #44
Implemented in #143
