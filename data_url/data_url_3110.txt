release-3.0 branch has a pyquil.external.rpcq file with RPCQ message definitions. Because the rpcq package defines message definitions for the RPCQ server, we should move those definitions to the RPCQ Python package.
