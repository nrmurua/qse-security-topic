Currently the way to force an argument to a specific type is giving it a default value. Since e.g. int32 and int64 behave differently, sometimes forcing the type of an argument is desirable.
We should have annotations instead of having to use default values.
