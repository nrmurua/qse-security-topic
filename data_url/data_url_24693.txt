Implement enter and exit routines so that a PulseStorage instance can be used in "with" blocks to replace the default pulse registry. Also implement a "set_default()" which conveniently allows to set any PulseStorage as global default pulse registry.
Depends on #276
I like this.
