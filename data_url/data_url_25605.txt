@AlejandroEsquivel , quick comment can we make the second part optional requirement ? i.e. user being able to file transfer without needling to have arg/kwargs (the code they are running might use the file in non pythonic interface). So something like
@santoshkumarradha the acceptance criteria was updated to remove the second requirement, so the corresponding PR only performs file transfer without the files arg. I will make a note of that however (making the files arg optional) for the upcoming task to add return values to CallDeps
