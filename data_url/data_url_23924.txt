Current we have a global configuration to choose between CNOT_simple and CNOT_CR. In principle we should be able to specify this in a more granular fashion. This also helps reduce magic that happens on from QGL import * since it currently happens once on import.
Closed by #233
